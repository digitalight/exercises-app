# Copy this file to your server along with the .env file
# Run with: docker compose -f docker-compose.server.yml up -d

services:
  backend:
    image: ${DOCKERHUB_USERNAME}/physio-tracker-backend:${IMAGE_TAG:-latest}
    container_name: physio-backend
    restart: unless-stopped
    environment:
      - PORT=3001
      - NODE_ENV=production
    volumes:
      - physio-data:/app/data
      - physio-uploads:/app/uploads

  frontend:
    image: ${DOCKERHUB_USERNAME}/physio-tracker-frontend:${IMAGE_TAG:-latest}
    container_name: physio-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  physio-data:
  physio-uploads:
